# 智能SQLer MVP开发计划

## 🎯 项目概述

智能SQLer是一款AI驱动的数据库管理工具，主要功能包括自然语言转SQL、查询执行、结果展示等。通过Svelte + Rust的技术栈组合，提供高性能、现代化的用户体验。

## 📝 开发原则

- **TDD测试驱动**: 所有功能必须先写测试
- **敏捷开发**: 每周迭代交付可用功能
- **代码质量**: 代码覆盖率100%，无警告
- **文档先行**: 关键设计必须先写文档

## 📅 12周开发计划

### 🏗️ Phase 1: 基础架构搭建 (Week 1-4)

#### Week 1: 项目初始化 & 环境配置

**目标**: 创建项目基础结构，配置开发环境

**任务清单**:
- [ ] 创建Git仓库和初始提交
- [ ] 配置Rust后端项目 (Axum + sqlx + tokio)
- [ ] 配置Svelte前端项目 (SvelteKit + TypeScript)
- [ ] 配置Tailwind CSS样式系统
- [ ] 创建项目目录结构
- [ ] 编写基础配置文件 (Cargo.toml, package.json, tsconfig.json)
- [ ] 配置开发工具链 (cargo-watch, Vite)
- [ ] 创建Makefile辅助脚本

**交付物**:
- 完整的项目骨架
- 前后端开发服务器可运行
- Hello World页面和API
- 开发文档草稿

**技术栈版本**:
```toml
# Rust依赖
axum = "0.7"
sqlx = { version = "0.7", features = ["runtime-tokio-rustls", "sqlite"] }
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
```

```json
// Svelte前端依赖
{
  "@sveltejs/kit": "^2.0.0",
  "typescript": "^5.3.0",
  "tailwindcss": "^3.4.0",
  "vite": "^5.0.0"
}
```

#### Week 2: 数据库抽象层

**目标**: 实现数据库连接和Schema获取功能

**Rust后端任务**:
- [ ] 定义DatabaseAdapter trait
- [ ] 实现SQLiteAdapter (支持内存数据库)
- [ ] 实现连接池管理 (Arc + SqlitePool)
- [ ] 实现get_schema()方法获取数据库结构
- [ ] 实现get_tables()获取表列表
- [ ] 实现get_columns()获取列信息
- [ ] 编写完整的单元测试 (覆盖率100%)

**关键代码**:
```rust
#[async_trait]
pub trait DatabaseAdapter: Send + Sync {
    async fn connect(&self) -> Result<(), DatabaseError>;
    async fn get_schema(&self) -> Result<DatabaseSchema, DatabaseError>;
    async fn get_tables(&self) -> Result<Vec<TableInfo>, DatabaseError>;
}

pub struct SQLiteAdapter {
    pool: Arc<sqlx::SqlitePool>,
    connection_string: String,
}
```

**测试覆盖**:
- 空数据库测试
- 表创建和Schema获取测试
- 列信息获取测试
- 错误处理测试

#### Week 3: 基础API和状态管理

**目标**: 实现后端API和前端状态管理

**Rust后端任务**:
- [ ] 创建Axum路由
- [ ] 实现/api/database/schema 端点
- [ ] 实现/api/database/tables 端点
- [ ] 实现错误处理中间件
- [ ] 编写API集成测试

**Svelte前端任务**:
- [ ] 创建Svelte Store (数据库连接状态)
- [ ] 实现API客户端 (fetch封装)
- [ ] 创建布局组件 (Header, Footer)
- [ ] 创建基础页面路由
- [ ] 实现数据库连接管理界面

**关键接口**:
```typescript
// stores/database-store.ts
interface DatabaseConnection {
    id: string;
    name: string;
    type: 'SQLite';
    path: string;
}

export const databaseConnections = writable<Map<string, DatabaseConnection>>(new Map());

// services/api-client.ts
class ApiClient {
    async getDatabaseSchema(): Promise<DatabaseSchema> {
        // 调用 /api/database/schema
    }
}
```

#### Week 4: AI服务集成

**目标**: 集成OpenAI API，实现自然语言转SQL

**Rust后端任务**:
- [ ] 创建AIService结构体
- [ ] 实现OpenAI客户端封装
- [ ] 实现提示词模板系统
- [ ] 实现/api/ai/generate-sql 端点
- [ ] 添加SQL注入防护
- [ ] 实现Schema感知的提示词生成
- [ ] 编写完整的AI功能测试

**关键代码**:
```rust
pub struct AIService {
    openai_client: OpenAIClient,
    prompt_template: PromptTemplate,
}

impl AIService {
    pub async fn generate_sql(&self, query: &str, schema: &DatabaseSchema) -> Result<GeneratedSQL, AIError> {
        let prompt = self.build_prompt(query, schema);
        self.openai_client.complete(prompt).await
    }
}
```

**测试重点**:
- 简单查询转换测试
- 多表关联查询测试
- SQL注入防护测试
- 错误处理测试

### 🤖 Phase 2: AI功能完善 (Week 5-7)

#### Week 5: SQL查询执行

**Rust后端任务**:
- [ ] 实现/api/database/execute 端点
- [ ] 实现QueryResult模型
- [ ] 实现SQL验证和清理
- [ ] 添加查询执行时间统计
- [ ] 实现查询结果序列化

**Svelte前端任务**:
- [ ] 创建SqlEditor组件
- [ ] 实现SQL编辑器UI
- [ ] 实现执行按钮和快捷键
- [ ] 实现加载状态显示
- [ ] 添加AI生成SQL的接口

**交互功能**:
- Ctrl+Enter执行查询
- Tab键缩进支持
- 语法高亮集成
- 查询历史管理

#### Week 6: 查询结果展示

**Rust后端任务**:
- [ ] 优化QueryResult数据结构
- [ ] 实现结果分页支持
- [ ] 添加行数限制配置
- [ ] 实现大结果集处理

**Svelte前端任务**:
- [ ] 创建QueryResults表格组件
- [ ] 实现响应式表格布局
- [ ] 实现列排序功能
- [ ] 实现数据导出 (CSV)
- [ ] 添加分页控制

**关键组件**:
```svelte
<!-- components/QueryResults.svelte -->
<script lang="ts">
    import type { QueryResult } from '$lib/types';

    export let result: QueryResult;

    // 排序逻辑
    function sortBy(column: string) {
        // 实现排序功能
    }
</script>

<div class="results-table">
    <table>
        <thead>
            <tr>
                {#each result.columns as col}
                    <th on:click={() => sortBy(col)}>{col}</th>
                {/each}
            </tr>
        </thead>
        <tbody>
            {#each result.rows as row}
                <tr>
                    {#each row as cell}
                        <td>{cell}</td>
                    {/each}
                </tr>
            {/each}
        </tbody>
    </table>
</div>
```

#### Week 7: 数据库结构浏览

**Rust后端任务**:
- [ ] 优化get_schema性能
- [ ] 添加表信息缓存
- [ ] 实现表行数统计
- [ ] 实现索引信息获取

**Svelte前端任务**:
- [ ] 创建DatabaseTree组件
- [ ] 实现树形结构展示
- [ ] 添加展开折叠功能
- [ ] 实现右键菜单
- [ ] 实现双击快速查询
- [ ] 优化大Schema性能

**交互功能**:
- 点击表显示列信息
- 双击表名生成SELECT查询
- 右键功能菜单
- 搜索过滤表

### 🎨 Phase 3: UI/UX完善 (Week 8-10)

#### Week 8: 布局优化

**Svelte前端任务**:
- [ ] 实现左右分屏布局
- [ ] 添加面板大小调整
- [ ] 实现响应式设计
- [ ] 添加主题切换
- [ ] 优化移动端适配
- [ ] 添加加载动画
- [ ] 实现错误提示Toast

**布局设计**:
```
┌──────────────────────────────────┐
│  Header (Logo + Connection)       │
├──────────────┬───────────────────┤
│              │                   │
│  Left Panel  │  Right Panel      │
│  (Database   │  (SQL Editor)     │
│   Tree)      │                   │
│              ├───────────────────┤
│              │                   │
│              │  Query Results    │
│              │                   │
└──────────────┴───────────────────┘
```

#### Week 9: 查询历史管理

**Rust后端任务**:
- [ ] 实现QueryHistory模型
- [ ] 添加历史记录存储 (SQLite)
- [ ] 实现历史查询获取API
- [ ] 实现历史清理功能

**Svelte前端任务**:
- [ ] 创建QueryHistory面板
- [ ] 实现历史查询列表
- [ ] 添加搜索过滤功能
- [ ] 实现点击复用历史查询
- [ ] 实现历史记录删除
- [ ] 添加收藏功能

#### Week 10: 性能优化

**Rust后端任务**:
- [ ] 添加连接池监控
- [ ] 实现查询缓存
- [ ] 添加慢查询日志
- [ ] 优化大数据量处理

**Svelte前端任务**:
- [ ] 实现虚拟滚动优化长列表
- [ ] 添加代码分割
- [ ] 实现组件懒加载
- [ ] 优化渲染性能

**性能目标**:
- 查询响应时间 < 2秒
- 前端加载时间 < 3秒
- 支持1000+行结果集
- 内存占用 < 512MB

### 🚀 Phase 4: 系统集成与发布 (Week 11-12)

#### Week 11: 系统集成测试

**集成任务**:
- [ ] 端到端功能测试
- [ ] API集成测试
- [ ] 前端组件测试
- [ ] 性能压测
- [ ] 安全性测试 (SQL注入等)
- [ ] 兼容性测试 (浏览器)
- [ ] 代码质量检查

**测试清单**:
- [ ] 100% 单元测试覆盖率
- [ ] 所有集成测试通过
- [ ] 端到端流程测试通过
- [ ] 性能测试达标
- [ ] 安全扫描无高危漏洞

**测试工具**:
```toml
# Rust测试
cargo test --all-features --verbose
cargo tarpaulin --out Html

# 前端测试
npm test -- --coverage --verbose
```

#### Week 12: 文档与发布

**文档任务**:
- [ ] 编写README文档
- [ ] 编写API文档 (OpenAPI spec)
- [ ] 编写用户手册
- [ ] 编写部署指南
- [ ] 创建演示视频

**发布任务**:
- [ ] 创建Docker镜像
- [ ] 编写Docker Compose配置
- [ ] 配置CI/CD流水线 (GitHub Actions)
- [ ] 发布v1.0.0版本
- [ ] 部署Demo环境

**Docker配置**:
```dockerfile
# Rust后端
FROM rust:1.75-alpine as builder
WORKDIR /app
COPY ./backend /app
RUN cargo build --release

FROM alpine:latest
RUN apk add --no-cache sqlite
COPY --from=builder /app/target/release/smart-sql /app/server
EXPOSE 3000
CMD ["/app/server"]
```

```dockerfile
# Svelte前端
FROM node:20-alpine as builder
WORKDIR /app
COPY ./frontend/package*.json ./
RUN npm install
COPY ./frontend /app
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/build /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

## 📊 每周交付物

| 周 | 核心功能 | 可演示效果 | 文档 |
|---|---|---|---|
| 1 | 项目骨架 | Hello World页面和API | 项目初始化文档 |
| 2 | 数据库连接 | 连接SQLite并获取Schema | 数据库层设计 |
| 3 | API基础 | 前端调用后端API | API规范 |
| 4 | AI功能 | 自然语言转SQL | AI集成文档 |
| 5 | 查询执行 | 执行SQL并显示结果 | 执行流程 |
| 6 | 结果展示 | 表格展示和数据导出 | 前端组件 |
| 7 | Schema浏览 | 树形结构浏览数据库 | 用户体验 |
| 8 | 布局优化 | 响应式布局和主题 | UI设计 |
| 9 | 历史管理 | 查询历史和收藏 | 功能增强 |
| 10 | 性能优化 | 虚拟滚动和缓存 | 性能文档 |
| 11 | 测试验证 | 完整功能测试 | 测试报告 |
| 12 | 发布准备 | 可部署的v1.0.0 | 完整文档 |

## 🎯 质量标准

### 代码质量
- **测试覆盖率**: 100%
- **代码审查**: 所有PR必须review
- **静态检查**: Clippy/ESLint无警告
- **格式化**: cargo fmt/prettier通过

### 功能质量
- **AI准确率**: > 85%
- **查询成功率**: > 95%
- **页面加载时间**: < 3秒
- **API响应时间**: < 200ms

### 文档质量
- **覆盖率**: 所有API都有文档
- **准确性**: 示例可运行
- **完整性**: 用户手册完整
- **时效性**: 随代码同步更新

## 🚀 风险评估与应对

### 技术风险
1. **AI调用成本高**
   - 应对: 缓存常见查询，实现查询去重
2. **Rust异步学习曲线**
   - 应对: 参考成熟模式，结对编程
3. **Svelte生态相对较小**
   - 应对: 使用成熟组件，必要时自建

### 进度风险
1. **Week 4 AI功能延期**
   - 应对: 简化提示词模板，后续迭代优化
2. **Week 8 UI设计延期**
   - 应对: 使用Tailwind默认主题，后续定制
3. **测试时间不足**
   - 应对: TDD模式，边开发边测试

### 质量风险
1. **AI生成SQL准确率低**
   - 应对: 提供Schema上下文，增加用户纠正
2. **性能不达标**
   - 应对: 增加缓存，优化数据加载策略
3. **安全漏洞**
   - 应对: 安全扫描，代码审查，渗透测试

## 📋 发布检查清单

### 开发完成
- [ ] 所有功能实现并通过测试
- [ ] 代码覆盖率100%
- [ ] 所有CI/CD管道通过
- [ ] 代码质量无警告

### 文档完成
- [ ] README文档完成
- [ ] API文档完整
- [ ] 用户手册完成
- [ ] 部署指南完成

### 发布准备
- [ ] Docker镜像构建成功
- [ ] Demo环境部署成功
- [ ] 性能测试通过
- [ ] 安全扫描通过
- [ ] 正式发布v1.0.0

**成功标准**: 完成所有核心功能，用户可通过自然语言生成SQL并执行查询，文档完整，可部署使用。
